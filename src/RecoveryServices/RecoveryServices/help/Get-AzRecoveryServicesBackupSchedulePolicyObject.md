### Example 1: The first command gets the retention policy object, and then stores it in the $RetPol variable.
```powershell
PS C:\> Get-AzRecoveryServicesBackupSchedulePolicyObject -WorkloadType AzureVM
```

