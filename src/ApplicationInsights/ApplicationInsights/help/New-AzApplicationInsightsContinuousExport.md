### Example 1: Create a new application insights continuous export configuration to export "Request" and "Trace" document types to storage contain "testcontainer" in storage account "teststorageaccount" in resource group "testgroup". The SAS token have to be valid and have write permission to the container, otherwise continuous export feature won't work.If SAS token expired, the continuous export feature will stop working. (autogenerated)
```powershell
PS C:\> New-AzApplicationInsightsContinuousExport -DocumentType Request -Name test -ResourceGroupName testgroup -StorageAccountId /subscriptions/00000000-0000-0000-0000-000000000000/resourceGroups/testgroup/providers/Microsoft.Storage/storageAccounts/teststorageaccount -StorageLocation sourcecentralus -StorageSASUri $sasuri
```

