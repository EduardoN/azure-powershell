### Example 1: This command adds a patch schedule to the cache named RedisCache06.
```powershell
PS C:\> New-AzRedisCachePatchSchedule -Entries <PSScheduleEntry[]> -Name RedisCache06 -ResourceGroupName ResourceGroup13
```

