### Example 1: The Set-AzVMPlan cmdlet sets the Azure Marketplace plan information for a virtual machine.
```powershell
PS C:\> Set-AzVMPlan -Name AgentPool01 -Product {Product} -Publisher $Publisher -VM $VirtualMachine
```

