### Example 1: Updates the state of a Vmss VM.  For now, the only allowed update is adding a managed data disk. (autogenerated)
```powershell
PS C:\> Update-AzVmssVM -VirtualMachineScaleSetVM $VmssVM
```

