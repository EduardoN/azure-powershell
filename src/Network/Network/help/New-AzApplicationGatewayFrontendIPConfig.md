### Example 1: The first command creates a public IP resource object and stores it in the $PublicIP variable.
```powershell
PS C:\> New-AzApplicationGatewayFrontendIPConfig -Name FrontEndConfig01 -PublicIPAddress $PublicIP
```

