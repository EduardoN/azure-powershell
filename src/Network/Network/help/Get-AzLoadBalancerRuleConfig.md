### Example 1: The first command gets the load balancer named MyLoadBalancer, and then stores it in the variable $slb.
```powershell
PS C:\> Get-AzLoadBalancerRuleConfig -LoadBalancer $slb -Name MyLBrulename
```

