### Example 1: The first command gets the application gateway named ApplicationGateway01 that belongs to the
```powershell
PS C:\> Set-AzApplicationGatewayFrontendPort -ApplicationGateway $AppGw -Name FrontEndPort01 -Port 80
```

