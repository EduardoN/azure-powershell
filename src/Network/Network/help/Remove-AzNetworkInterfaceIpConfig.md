### Example 1: The first command gets a network interface called mynic and stores it in the variable $nic. The second command
```powershell
PS C:\> Remove-AzNetworkInterfaceIpConfig -Name IPConfig-1 -NetworkInterface $nic
```

