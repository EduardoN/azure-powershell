### Example 1: The first command gets the load balancer named MyLoadBalancer, and then stores it in the $loadbalancer variable.
```powershell
PS C:\> Remove-AzLoadBalancerRuleConfig -LoadBalancer $loadbalancer -Name MyLBruleName
```

