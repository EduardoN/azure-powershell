### Example 1: This cmdlet returns a list of machine runs. Each software update run will trigger a machine run for each of the software update configuration target machine. To get a specific machine run, pass the Id parameter. You can list all the machine runs, all runs for a specific computer, all runs with specific status by passing the corresponding parameters. (autogenerated)
```powershell
PS C:\> Get-AzAutomationSoftwareUpdateMachineRun -AutomationAccountName AutomationAccount01 -ResourceGroupName MyResourceGroup -TargetComputer {TargetComputer}
```

