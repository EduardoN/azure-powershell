### Example 1: The first command uses the Get-AzDnsRecordSet cmdlet to get the specified record set, and then stores it in the $RecordSet variable.
```powershell
PS C:\> Set-AzDnsRecordSet -RecordSet $RecordSet
```

