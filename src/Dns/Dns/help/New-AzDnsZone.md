### Example 1: This command creates a new DNS zone named myzone.com in the specified resource group, and then
```powershell
PS C:\> New-AzDnsZone -Name mychild.zone.com -ResourceGroupName MyResourceGroup
```

